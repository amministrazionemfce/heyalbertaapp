MONGO_URL=mongodb://localhost:27017/hey_alberta_pro
JWT_SECRET=change_me
PORT=8000

CORS_ORIGINS=http://localhost:3000
GOOGLE_MAPS_API_KEY=

# --- Email (admin “Send email”, marketing) — required for real sends ---
# SMTP_HOST=smtp.your-provider.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
# MAIL_FROM="Hey Alberta <noreply@yourdomain.com>"
#
# Local dev without SMTP: messages are logged, not delivered (never enable in production).
# SMTP_MOCK=true
